services:
  postgres:
    image: 'postgres:latest'
    container_name: ecommercedb
    environment:
      - 'POSTGRES_DB=ecommercedb'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'